# Local dev: SQLite (no server to install). For production PostgreSQL, switch provider in prisma/schema.prisma.
DATABASE_URL="file:./dev.db"
JWT_SECRET="replace-with-a-long-random-string"
JWT_EXPIRES="7d"
PORT=3000
